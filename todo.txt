Create conda env with all required packages (+ compatible versions)

Create notebooks
Compile from notebooks

Create command line tool to run
Add requirements - needs either old dearpygui OR update to new syntax
Create installable package

Document